Goal: Design a ui component to present 3D relationship of the knowledges in the scheme: `knowledges`, and `knowledge_type`

use the components in `/home/avarile/Documents/codeRepo/ai-agent-cluster/frontend/app/data-links-modal` as references. But not all features in the refence needs to be implemented.

the `knowledge_type` will be the core node of all knowledges of it kind.
all nodes of `knowledge_type` will be connecting to the static node `knowledge_core`

based on the references, design a dedicated backend module to provide the APIs and build the state management ans ui in the frontend accordingly.

this design must be clean, clear, professional and production ready.

output the design to scripts/customized/developments/current-design.md.