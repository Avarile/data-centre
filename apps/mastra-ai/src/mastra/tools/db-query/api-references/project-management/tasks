# Table: tasks

## API Operations

### 1. Read Records (GET)
```bash
curl -X GET "https://projects.avarile.com/api/table/tblOMgDiajqa1moRRjE/record?fieldKeyType=name" \
  -H "Authorization: Bearer <YOUR_API_TOKEN>"
```

#### Pagination
Use `skip` and `take` parameters:
- `take`: Number of records to return (default: 100, max: 1000)
- `skip`: Number of records to skip

```bash
# Get 20 records, starting from the 41st record (page 3)
curl "https://projects.avarile.com/api/table/tblOMgDiajqa1moRRjE/record?take=20&skip=40&fieldKeyType=name" \
  -H "Authorization: Bearer <YOUR_API_TOKEN>"
```

#### Filtering
Use the `filter` parameter with a JSON object.

**⚠️ Important: The `fieldId` in filter/orderBy MUST use the actual field ID (e.g., "fldXXXX"), not the field name.**

```bash
# Filter records - use field ID from the Fields section above
curl "https://projects.avarile.com/api/table/tblOMgDiajqa1moRRjE/record?fieldKeyType=name" \
  --data-urlencode 'filter={"conjunction":"and","filterSet":[{"fieldId":"fldXXXXXXX","operator":"is","value":"Active"}]}' \
  -H "Authorization: Bearer <YOUR_API_TOKEN>"
```

**Filter Operators**:
- Text: `is`, `isNot`, `contains`, `doesNotContain`, `isEmpty`, `isNotEmpty`
- Number: `is`, `isNot`, `isGreater`, `isLess`, `isGreaterEqual`, `isLessEqual`
- Date: `is`, `isBefore`, `isAfter`, `isWithin`

#### Sorting
Use the `orderBy` parameter.

**⚠️ Important: The `fieldId` in orderBy MUST use the actual field ID (e.g., "fldXXXX"), not the field name.**

```bash
# Sort by a field - use field ID from the Fields section above
curl "https://projects.avarile.com/api/table/tblOMgDiajqa1moRRjE/record?fieldKeyType=name" \
  --data-urlencode 'orderBy=[{"fieldId":"fldXXXXXXX","order":"desc"}]' \
  -H "Authorization: Bearer <YOUR_API_TOKEN>"
```

#### Field Selection (Projection)
Use the `projection` parameter to return only specific fields:
```bash
# Only return "Name" and "Email" fields
curl "https://projects.avarile.com/api/table/tblOMgDiajqa1moRRjE/record?fieldKeyType=name&projection=Name&projection=Email" \
  -H "Authorization: Bearer <YOUR_API_TOKEN>"
```

#### Searching
Use the `search` parameter:
```bash
# Search for "john" in all fields
curl "https://projects.avarile.com/api/table/tblOMgDiajqa1moRRjE/record?search=john&fieldKeyType=name" \
  -H "Authorization: Bearer <YOUR_API_TOKEN>"
```

### 2. Create Record (POST)
```bash
curl -X POST "https://projects.avarile.com/api/table/tblOMgDiajqa1moRRjE/record" \
  -H "Authorization: Bearer <YOUR_API_TOKEN>" \
  -H "Content-Type: application/json" \
  -d '{
    "fieldKeyType": "name",
    "records": [
      {
        "fields": {
          // Editable fields: "title", "context", "deleted_at", "is_active", "progress", "priority", "assigned_to", "started_at", "finished_at", "belong_project"
        }
      }
    ]
  }'
```

### 3. Update Record (PATCH)
```bash
curl -X PATCH "https://projects.avarile.com/api/table/tblOMgDiajqa1moRRjE/record/{recordId}" \
  -H "Authorization: Bearer <YOUR_API_TOKEN>" \
  -H "Content-Type: application/json" \
  -d '{
    "fieldKeyType": "name",
    "record": {
      "fields": {
        // Include only fields you want to update
      }
    }
  }'
```

### 4. Delete Record (DELETE)
```bash
curl -X DELETE "https://projects.avarile.com/api/table/tblOMgDiajqa1moRRjE/record/{recordId}" \
  -H "Authorization: Bearer <YOUR_API_TOKEN>"
```

---

## API Configuration
- **Base URL**: https://projects.avarile.com
- **Table ID**: tblOMgDiajqa1moRRjE
- **API Token**: <YOUR_API_TOKEN>
- **Endpoint**: `https://projects.avarile.com/api/table/tblOMgDiajqa1moRRjE/record`

## Authentication
All requests require the `Authorization` header:
```
Authorization: Bearer <YOUR_API_TOKEN>
```

---

## Fields
  - "title" [id: fldGqUoXO7oyq6ufX2Y] (Single line text) [PRIMARY]
  - "context" [id: fldaOhjcXqdiF3IRVB1] (Long text / Rich text)
  - "created_at" [id: fldYNEyagDaFRoxV8zF] (Created time (auto-generated)) [READ-ONLY]
  - "updated_at" [id: fldVlGp4CjCeiNLC6cb] (Last modified time (auto-generated)) [READ-ONLY]
  - "deleted_at" [id: fldkdTbYu0f3LaYK7wl] (Date/Time)
  - "is_active" [id: fldJMclfBagyBukSxoy] (Checkbox (true/false))
  - "id" [id: fld2s647Pe9XgapERLS] (Auto-incrementing number) [READ-ONLY]
  - "progress" [id: fldG7fZN9XhOa0lMy33] (Single select (options: backlog, in-progress, finished_reviewing, finished_validating, onhold, cancelled))
  - "priority" [id: fldMTuydiWUFAgtqAPX] (Single select (options: urgent, important, prioritise, normal, can wait))
  - "assigned_to" [id: fld3ZGyfGzogzwHt5Md] (Link to another table)
  - "started_at" [id: fldj3Xb1g9nUX4iFqE0] (Date/Time)
  - "finished_at" [id: fldn95wbkTqefn18WdO] (Date/Time)
  - "belong_project" [id: fld6X3nrMTQlYiV5XSa] (Link to another table)

---

## Notes for AI
- Fields marked [PRIMARY] are the main identifier field
- Fields marked [READ-ONLY] are computed and cannot be directly modified
- Use `fieldKeyType=name` to reference fields by their display name in request/response body
- **Important**: `filter` and `orderBy` parameters MUST use field IDs (the [id: fldXXX] shown above), not field names
- Dates should be in ISO 8601 format (e.g., "2024-01-15T10:30:00Z")
- For select fields, use the exact option names listed above
- For link fields, provide an array of record IDs from the linked table
- Response format: `{ "records": [{ fields: { ... } }] }`
